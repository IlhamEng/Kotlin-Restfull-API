version: '3.5'

services:
  kotlin-restful-api-postgres:
    container_name: "kotlin-restful-api-postgres"
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: restful@kotlin
      POSTGRES_USER: restful@kotlin
      POSTGRES_DB: restful-api-kotlin
